function inputes and parameter and argurments:
==============================================

def greet(name): # we call this "name" which is defined in the function defining "parameter"
    print(f"hello {name}")
    print("how do you do?")
    print("isn't whether is nice ?")

greet("rakshith") #rakshith called as a argument
hi